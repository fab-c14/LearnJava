public class Polymorphism {
    public static void main(String[] args) {
        // it has two words, poly means many , morphism - means behaviour,
        // so it means differnt behaviours, take an example of human - boss, employeee,
        // husband, son (one object but differnt behaviours)
        // there are two types of polymorphism, compile time(early binding), runtime
        // (late binding) -

        // overloading(compile timing), overriding (runtime),
    }
}
